version: '2'

services:
    php:
        build:
            context: .
            dockerfile: docker/php/Dockerfile
            args:
                TIMEZONE: ${TIMEZONE}
        ports:
            - 9000:9000
        volumes:
            - ./app:/var/www/html/app
        networks:
            - symfony

networks:
    symfony:
